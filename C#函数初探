对于C#的函数来讲，类似于foreach，for循环的函数来说，类似于for（int i=0）的情况是允许出现的，此时如果将鼠标移动到int i这行语句之上，就会出现局部变量的信息，因此
对于c语言来讲，这些地方都是可以接受的，而不会出现类似于C语言中没有初始化变量i的情况。

对于一些转换类型的数组，往往是类似于ToInt32（i+j），是先将j和i转换为字符，在运行+，也就是拼接符是，优先计算括号内的字母，并不是拼接，因为括号的优先级最高。
